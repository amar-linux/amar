#include<stdio.h>


int main(){

    int a= 8;
    int b =7;

    // if we do the xor of an b we will get the result 
    // number of 1's in the result actually is the number of bits that were differnt in a and b 
    // xor of two different bits will give o/p as 1
    // and then we have to count the number of bits
}
